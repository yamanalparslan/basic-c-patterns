#include <stdio.h>
int main(void)
{
int h,i,j;
printf("enter the width you want(integeer):");
scanf("%d",&h);
    for(i=1;i<=h;i++)
    {
        for(j=1;j<=i;j++)
        {
            printf("%d",j);
        }
        printf("\n");
}
return 0 ;
}
